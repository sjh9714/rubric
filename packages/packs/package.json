{
  "name": "@rubric-dev/packs",
  "version": "0.0.0",
  "private": true,
  "description": "Built-in rule packs for rubric.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "builtins"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run tests"
  },
  "dependencies": {
    "@rubric-dev/core": "workspace:*",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
