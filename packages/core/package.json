{
  "name": "@rubric-dev/core",
  "version": "0.0.0",
  "private": true,
  "description": "Core engine for rubric.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run tests"
  },
  "dependencies": {
    "picomatch": "^4.0.4",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/picomatch": "^4.0.3"
  }
}
